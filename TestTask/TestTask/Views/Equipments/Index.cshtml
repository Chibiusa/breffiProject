@model IEnumerable<TestTask.ModelsFromDB.Equipment>

    <p>
        @Html.ActionLink("Добавить", "Create")
    </p>
    <table class="table w3-table w3-bordered w3-striped  w3-hoverable">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdBrand)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdToolType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.IdBrand)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdToolType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.ActionLink("Изменить", "Edit", new { id=item.IdEquipment }) |
                @Html.ActionLink("Просмотреть", "Details", new { id=item.IdEquipment }) |
                @Html.ActionLink("Удалить", "Delete", new { id=item.IdEquipment })
            </td>
        </tr>
    }
    
    </table>
